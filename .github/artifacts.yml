# SPDX-FileCopyrightText: 2025 diggsweden/wallet-backend-reference
#
# SPDX-License-Identifier: CC0-1.0

# Artifacts Configuration for wallet-backend-reference
# Defines what to build and where to publish

artifacts:
  - name: wallet-backend-reference
    project-type: maven
    working-directory: .
    build-type: application
    config:
      java-version: 21

containers:
  - name: wallet-backend-reference
    from: [wallet-backend-reference]
    container-file: Containerfile
    context: .
    platforms: linux/amd64,linux/arm64
    registry: ghcr.io

publishers:
  - type: github-packages
    artifacts: [wallet-backend-reference]
